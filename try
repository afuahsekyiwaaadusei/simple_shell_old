#include <stdio.h>
#include <stdlib.h>
#include "main.h"
#include <unistd.h>

/**
 *main - main function used for testing
 *
 *Return: always 0.
 */

int main() {
    char *line ="                          ";
    int count = get_arg_no(line);
    
    printf("count is %d\n", count);
    return 0;
}
